(()=>{'use strict';const DATA_URL='collections/uk-motorway-services-v1.json',FUEL_MARK='⛽';let renderedLayer=null,servicesPromise=null;const escapeHtml=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));async function loadServices(){if(!servicesPromise)servicesPromise=fetch(DATA_URL).then(response=>{if(!response.ok)throw new Error('Service-station catalogue could not be loaded.');return response.json();}).then(data=>Array.isArray(data?.services)?data.services:[]);return servicesPromise;}function pinIcon(){return L.divIcon({className:'roadprints-service-station-icon',html:'<span class="roadprints-service-station-pin" aria-hidden="true"><span>'+FUEL_MARK+'</span></span>',iconSize:[31,31],iconAnchor:[15,30],popupAnchor:[0,-28]});}async function render(context){const layer=context?.serviceStationLayer;if(!layer||!window.L||renderedLayer===layer)return;try{const services=await loadServices();if(renderedLayer===layer)return;layer.clearLayers();const icon=pinIcon();services.forEach(service=>{const lat=Number(service?.lat),lng=Number(service?.lng);if(!Number.isFinite(lat)||!Number.isFinite(lng))return;const name=escapeHtml(service.name||'Motorway service station'),operator=service.operator?'<p>'+escapeHtml(service.operator)+'</p>':'';L.marker([lat,lng],{icon,title:(service.name||'Motorway service station')+' — unvisited'}).bindPopup('<div class="service-station-popup"><h3>'+name+'</h3><p>Unvisited motorway service station</p>'+operator+'</div>').addTo(layer);});renderedLayer=layer;}catch(error){console.warn('Roadprints service-station layer unavailable:',error);}}window.addEventListener('roadprints:map-ready',event=>render(event.detail));if(window.roadprintsMapContext)render(window.roadprintsMapContext);})();
